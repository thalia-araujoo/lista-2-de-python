numeros = list()
nova_lista = []
for i in range(0,11):
    num = int(input("Digite os números: "))
    numeros.append(num)

for i in range(len(numeros)):
    numeros[i] = numeros[i]*2
nova_lista = numeros
print(nova_lista)
